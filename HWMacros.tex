% Short for epsilon - we'll be writing it enough...
\let\ep\epsilon
% Set up implies with the right alignment
\def\imp{\implies &}
% "No Implies" - align without the implies
% Use to align in eqnarray* blocks that contain \imp
\def\nimp{&}
% Equals with alignment
\def\eq{=&}
% New line with equals alignment
\def\next{\\\eq}
% Box to show we have a finished proof
\def\done{$\Box$}
